{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Formateando con Prettier...",
            "timeout": 30,
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{try{const j=JSON.parse(d);const f=(j.tool_response&&j.tool_response.filePath)||(j.tool_input&&j.tool_input.file_path)||'';if(f)console.log(f)}catch(e){}})\" | { read -r f; case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/core-harness/*) ;; \"$CLAUDE_PROJECT_DIR\"/*) if [ -x \"$CLAUDE_PROJECT_DIR/node_modules/.bin/prettier\" ]; then cd \"$CLAUDE_PROJECT_DIR\" && ./node_modules/.bin/prettier --write --ignore-unknown \"$f\" || echo \"hook de formato: prettier no pudo con $f\" >&2; fi;; esac; }"
          }
        ]
      }
    ]
  }
}
