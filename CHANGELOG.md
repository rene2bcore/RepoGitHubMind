# Changelog

<!-- Este fichero se genera desde los commits convencionales (`feat:`, `fix:`, `feat!:`) con la herramienta que se decida al publicar la primera versión. No se edita a mano. Hasta entonces, `git log --oneline` es el changelog. -->

Sin versiones publicadas todavía.
